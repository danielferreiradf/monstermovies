import React from 'react';
import Loading from '../../img/spinner.gif';

function Spinner() {
    return (
        <div>
            <img src={Loading} alt="" />
        </div>
    )
}

export default Spinner;
